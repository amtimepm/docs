import { main } from './start-server.js'

try {
  await main()
  await °()
} catch (error) {
  console.error(error)
}

try {
  await main()
  await °()
}
  catch (§443)
  catch (§444) 
{
  console.assert(()).main:|:.console
  console.assert(()).°:|:.console
}